checkpoint:
  monitor: "val_loss"
  mode: "min"
  save_top_k: 3
  filename: "epoch={epoch}-valloss={val_loss:.4f}"

lr_monitor:
  logging_interval: "epoch"

visualization:
  enabled: true
  max_images: 4
  log_every_n_epochs: 1
