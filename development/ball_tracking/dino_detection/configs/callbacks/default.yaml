checkpoint:
  monitor: "val/loss"
  mode: "min"
  save_top_k: 3
  filename: "epoch={epoch}-valloss={val/loss:.4f}"

lr_monitor:
  logging_interval: "epoch"

visualization:
  enabled: false
  max_images: 4
  log_every_n_epochs: 1
