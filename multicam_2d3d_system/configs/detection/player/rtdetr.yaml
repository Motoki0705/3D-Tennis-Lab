# RT-DETR player detector configuration.

weights: ${workspace.root}/trained_models/player_analysis/rt_detr/rt_detr.ckpt
pretrained_model_name_or_path: PekingU/rtdetr_v2_r18vd
num_labels: 1
device: cuda
strict: false
remove_prefix: "model."
allow_partial: true
torch_compile: false
score_threshold: 0.5
batch_size: 16
use_half: true
